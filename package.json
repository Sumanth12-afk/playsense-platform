{
  "name": "playsense",
  "version": "1.0.0",
  "private": true,
  "description": "Privacy-First Parental Gaming Insights Platform",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=packages/web",
    "dev:desktop": "npm run dev --workspace=packages/desktop",
    "build:shared": "npm run build --workspace=packages/shared",
    "build:web": "npm run build:shared && npm run build --workspace=packages/web",
    "build:desktop": "npm run build --workspace=packages/desktop",
    "build:all": "npm run build:shared && npm run build --workspace=packages/web && npm run build:desktop",
    "supabase": "supabase",
    "supabase:deploy:functions": "supabase functions deploy send-weekly-digest && supabase functions deploy send-test-email"
  },
  "devDependencies": {
    "supabase": "^2.15.0",
    "typescript": "^5.3.3"
  }
}

